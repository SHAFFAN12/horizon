/* --- RESPONSIVE STYLES --- */

@media (max-width: 1024px) {
    /* Tablet & Small Laptop Adjustments */

    /* Global Adjustments */
    .fnc-slide__heading-line {
        font-size: 4rem;
    }

    /* Slide 1 Specifics */
    .slide-logo {
        width: 220px;
        top: 30px;
        left: 30px;
    }

    .example-slider .fnc-slide-1 .fnc-slide__content {
        width: 80%;
    }

    /* Hide mask on tablet for Slide 1 to prevent layout fracture */
    .example-slider .fnc-slide-1 .fnc-slide__mask,
    .example-slider .fnc-slide-1 .fnc-slide__mask-inner {
        opacity: 0 !important;
        visibility: hidden !important;
    }

    .example-slider .fnc-slide-1 .fnc-slide__inner {
        background-image: url("assets/tablet_hero.png") !important;
        background-position: center center !important;
    }
}

@media (max-width: 768px) {

    /* Global Adjustments */
    .fnc-slide__heading-line {
        font-size: 3rem;
        padding-right: 0;
    }

    .fnc-slide__heading-line:nth-child(2) {
        padding-left: 0;
    }

    .slide-text-content {
        font-size: 1.1rem;
        max-width: 90%;
        margin-top: 15px;
    }

    .fnc-slide__action-btn {
        margin-left: 0;
        font-size: 1rem;
        padding: 10px 25px;
    }

    /* Logo Scaling */
    .slide-logo {
        width: 150px;
        top: 20px;
        left: 20px;
    }

    /* Navigation Adjustments */
    .fnc-nav {
        width: 100%;
        border-radius: 0;
        bottom: 0;
        left: 0;
    }

    .fnc-nav__controls {
        justify-content: space-around;
        padding: 5px;
    }

    .fnc-nav__control {
        min-width: auto;
        font-size: 10px !important;
        padding: 5px;
        letter-spacing: 0.5px;
    }

    /* Projects Slide Enhancements for Mobile */
    .project-slide-content {
        top: 5% !important;
        padding-top: 60px;
        /* Space for logo */
    }

    .project-slide-content .fnc-slide__heading {
        margin-bottom: 20px;
    }

    .project-slide-content .fnc-slide__heading-line {
        font-size: 2.5rem;
        /* Smaller for mobile to fit */
        letter-spacing: 2px;
    }

    /* Vertical Central Tree for Mobile */
    .timeline-container {
        height: auto;
        width: 100%;
        display: block;
        margin-top: 10px;
        padding-bottom: 60px;
        overflow-y: auto;
        max-height: 65vh;
        position: relative;
        perspective: 1000px;
    }

    .timeline-line {
        display: none !important;
        /* Hide original fixed line */
    }

    .timeline-items::before {
        content: '';
        position: absolute;
        left: 50%;
        transform: translateX(-50%);
        top: 0;
        bottom: 150px;
        /* Stop the line early before the coming soon box */
        width: 4px;
        background: linear-gradient(to bottom,
                transparent 0%,
                var(--color-gold) 5%,
                var(--color-gold) 100%);
        box-shadow: 0 0 10px rgba(212, 175, 55, 0.4);
        z-index: 1;
    }

    .timeline-items {
        display: flex;
        flex-direction: column;
        width: 100%;
        padding: 20px 0;
        position: relative;
    }

    .timeline-item {
        width: 40%;
        /* Reduced from 42% to prevent overflow */
        margin: 10px 0 !important;
        position: relative;
        transform: none !important;
    }

    /* Odd Items - LEFT Side */
    .timeline-item:nth-child(odd) {
        align-self: flex-start;
        margin-left: 2%;
        text-align: right;
    }

    /* Even Items - RIGHT Side */
    .timeline-item:nth-child(even) {
        align-self: flex-end;
        margin-right: 2%;
        text-align: left;
    }

    /* Recalculated Connectors for 40% Width */
    /* Center (50%) - (Width 40% + Margin 2%) = 8% Gap */
    /* Making connector 10% to ensure overlap */

    .timeline-item:nth-child(odd)::after {
        content: '';
        position: absolute;
        top: 50%;
        right: -10%;
        width: 10%;
        height: 2px;
        background: var(--color-gold);
        display: block !important;
        transform: translateY(-50%);
    }

    .timeline-item:nth-child(even)::before {
        content: '';
        position: absolute;
        top: 50%;
        left: -10%;
        width: 10%;
        height: 2px;
        background: var(--color-gold);
        display: block !important;
        transform: translateY(-50%);
    }

    .timeline-content {
        flex-direction: column;
        align-items: center;
        padding: 8px;
        width: 100%;
        background: rgba(255, 255, 255, 0.08);
        backdrop-filter: blur(15px);
    }

    .timeline-content img {
        width: 100%;
        height: 60px;
        margin-right: 0;
        margin-bottom: 5px;
    }

    .timeline-content span {
        font-size: 0.8rem;
        font-weight: 600;
        line-height: 1.2;
        text-align: center;
        margin-top: 5px;
    }

    /* Upcoming Node Styling */
    .timeline-item-upcoming {
        align-self: center !important;
        margin: 40px auto 20px !important;
        width: 60%;
        text-align: center !important;
        z-index: 10;
        position: relative;
    }

    /* Dedicated connector line from the main timeline */
    .timeline-item-upcoming::before {
        content: '' !important;
        display: block !important;
        position: absolute;
        top: -40px;
        /* Bridge gap perfectly */
        left: 50%;
        transform: translateX(-50%);
        width: 4px;
        height: 40px;
        background: var(--color-gold);
        box-shadow: 0 0 10px rgba(212, 175, 55, 0.4);
    }

    .timeline-item-upcoming::after {
        display: none !important;
    }

    .upcoming-content {
        background: rgba(0, 31, 63, 0.95);
        /* Increased opacity to hide timeline line */
        border: 1px solid rgba(212, 175, 55, 0.4);
        /* Solid soft gold border instead of dashed */
        backdrop-filter: blur(16px);
        /* Strong glass blur */
        -webkit-backdrop-filter: blur(16px);
        box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.4), inset 0 0 15px rgba(212, 175, 55, 0.05);
        /* Deep shadow with soft gold inner glow */
        padding: 20px 15px;
        border-radius: 16px;
        position: relative;
        overflow: hidden;
    }

    /* Adds a subtle sweeping shine across the upcoming node */
    .upcoming-content::after {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 50%;
        height: 100%;
        background: linear-gradient(to right, transparent, rgba(255, 255, 255, 0.08), transparent);
        transform: skewX(-25deg);
        animation: shine 4s infinite;
    }

    @keyframes shine {
        0% {
            left: -100%;
        }

        20% {
            left: 200%;
        }

        100% {
            left: 200%;
        }
    }

    .upcoming-content i {
        font-size: 2.2rem;
        color: var(--color-gold);
        margin-bottom: 10px;
        display: block;
        text-shadow: 0 0 15px rgba(212, 175, 55, 0.6);
    }

    /* Modal Mobile Tweaks */
    .project-modal__container {
        width: 95%;
        max-height: 85vh;
        border-radius: 15px;
    }

    .project-modal__details {
        padding: 25px;
    }

    .project-modal__details h2 {
        font-size: 1.3rem;
        /* Further reduced to fit long titles */
        margin-top: 15px;
        /* Added margin to avoid overlap with image nav */
        margin-bottom: 20px;
        text-align: center;
        line-height: 1.3;
    }

    .modal-grid {
        gap: 15px;
    }

    .project-modal__close {
        top: 10px;
        right: 15px;
        font-size: 30px;
    }

    .project-modal__nav button {
        width: 40px;
        height: 40px;
    }

    /* Slide 1 Image & Text Position */
    .example-slider .fnc-slide-1 .fnc-slide__content {
        width: 60%;
        top: 50%;
    }

    /* Slide 4 Contacts */
    .fnc-slide__heading {
        margin-bottom: 15px;
    }

    /* --- FIX SLIDE 1 FRACTURE ON MOBILE --- */
    .example-slider .fnc-slide-1 .fnc-slide__mask,
    .example-slider .fnc-slide-1 .fnc-slide__mask-inner {
        opacity: 0 !important;
        visibility: hidden !important;
        /* Completely hide mask to prevent tile effect */
    }

    .example-slider .fnc-slide-1 .fnc-slide__inner {
        background-image: url("assets/mobile_hero.png") !important;
        background-size: cover !important;
        background-position: center center !important;
        background-repeat: no-repeat !important;
        background-attachment: scroll !important;
        background-blend-mode: normal !important;
    }


    /* --- CONTACT SLIDE RESPONSIVE --- */
    .glass-card-container {
        width: 95%;
        max-width: 95%;
        padding: 20px;
        margin: 80px auto 20px;
        /* Center and add top margin for logo */
        max-height: calc(100vh - 120px);
        overflow-y: auto;
        position: relative;
        z-index: 5;
    }

    .contact-title {
        font-size: 2rem;
        margin-bottom: 5px;
    }

    .contact-subtitle {
        font-size: 0.9rem;
        margin-bottom: 20px;
    }

    .glass-card-content {
        flex-direction: column;
        /* Stack form and info */
        gap: 20px;
    }

    /* Form Adjustments */
    .glass-form {
        width: 100%;
        padding-right: 0;
        border-right: none;
        border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        padding-bottom: 20px;
    }

    .form-row {
        flex-direction: column;
        gap: 10px;
    }

    .glass-input {
        padding: 12px;
        font-size: 0.9rem;
    }

    .glass-textarea {
        height: 80px;
    }

    .glass-submit-btn {
        width: 100%;
        padding: 12px;
        font-size: 1rem;
    }

    /* Info Section Adjustments */
    .glass-info {
        width: 100%;
        padding-left: 0;
        gap: 15px;
    }

    .info-item {
        font-size: 0.9rem;
    }

    .info-icon {
        width: 30px;
        height: 30px;
        line-height: 30px;
        font-size: 0.9rem;
    }

    .footer-map {
        height: 150px;
        object-fit: cover;
    }

    /* --- ABOUT US RESPONSIVE --- */
    .about-slide-content {
        width: 100%;
        padding: 20px !important;
        top: 80px !important;
        /* Start below mobile logo */
        transform: translateX(-50%) !important;
        max-height: calc(100vh - 140px);
        overflow-y: auto;
    }

    .about-slide-content .fnc-slide__heading-line {
        font-size: 2.5rem;
        margin-top: 0;
        line-height: 1.1;
        letter-spacing: -1px;
    }

    .about-main-layout {
        gap: 30px;
        margin-bottom: 40px;
    }

    .about-description p {
        font-size: 1.1rem;
        padding: 0;
    }

    .about-mission {
        padding: 20px;
    }

    .about-features {
        grid-template-columns: 1fr;
        gap: 20px;
        padding-bottom: 20px;
    }

    .feature-card {
        padding: 25px 20px;
    }
}

/* CEO Section Responsive */
@media (max-width: 768px) {
    .about-ceo-section {
        grid-template-columns: 1fr;
        gap: 30px;
        padding: 30px 20px;
        margin: 40px 0;
        text-align: center;
    }

    .ceo-image-card {
        max-width: 280px;
        margin: 0 auto;
    }

    .ceo-name {
        font-size: 1.8rem;
    }

    .ceo-journey {
        font-size: 1rem;
    }

    .portfolio-tags {
        justify-content: center;
    }
}